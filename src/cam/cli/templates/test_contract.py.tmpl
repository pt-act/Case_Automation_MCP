"""Contract tests for the {name} connector.

These tests verify the adapter conforms to the Connector protocol.
Fill in the TODO markers and remove the skip when ready.
"""

from __future__ import annotations

import pytest

pytestmark = pytest.mark.skip(reason="{name} adapter not yet implemented")


# TODO: Import your adapter and the contract harness
# from cam.connectors.{name}.adapter import {name_title}Adapter
# from tests.connectors.harness import run_contract


# TODO: Implement contract tests
# async def test_contract_case() -> None:
#     adapter = {name_title}Adapter(...)
#     await run_contract(case=adapter)
