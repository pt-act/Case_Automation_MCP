"""Consulting/advisory reference domain pack.

The proof pack: a minimal, recognisable non-immigration practice that runs the
same engine (intake -> QC -> deadline -> routing) with different terminology,
case types, document classes, and a client-confidential restriction policy.
Demonstrates that adding a domain requires no engine/core edits.
"""

from __future__ import annotations

from cam.packs.consulting.pack import CONSULTING_PACK

__all__ = ["CONSULTING_PACK"]
